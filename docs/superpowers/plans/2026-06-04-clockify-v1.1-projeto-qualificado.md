# Clockify v1.1 — Resolução por (Projeto, Tarefa) + Histórico — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Resolver tarefa por `(projeto, tarefa)` (mantendo fallback por nome único) e aprender mapeamentos recorrentes num histórico local que sugere na conversa — destravando colegas multi-cliente cujos nomes de tarefa se repetem entre projetos.

**Architecture:** Mantém a separação cérebro/IO. O motor (`entries.py`) ganha resolução project-aware; modelos e config ganham um campo `project` opcional; um módulo novo `history.py` faz I/O determinístico do histórico; o `add` grava o histórico no sucesso; `suggest` o expõe; os slash commands consultam `suggest` e aplicam precedência override > histórico > default. Tudo retrocompatível (campos opcionais).

**Tech Stack:** Python 3.12, argparse, httpx, pytest + respx, `uv`. Plugin Claude Code (`plugins/clockify-horas/`).

**Spec:** `docs/superpowers/specs/2026-06-04-clockify-v1.1-projeto-qualificado-design.md`

**Base:** repo em `master`. **Trabalhe em um branch novo** `feat/v1.1-projeto-qualificado` (não implemente em master). Todos os caminhos abaixo são relativos à raiz do repo; o projeto Python vive em `plugins/clockify-horas/` — rode `uv run pytest` de lá.

---

## File Structure

**Criar:**
- `plugins/clockify-horas/src/clockify_horas/history.py` — I/O do histórico (path, read/write, record, suggest)
- `plugins/clockify-horas/tests/test_history.py` — testes do histórico

**Modificar:**
- `src/clockify_horas/models.py` — `TimeEntry.project_name`
- `src/clockify_horas/entries.py` — `_resolve_task(project_name)`, `build_payload`, `from_event`
- `src/clockify_horas/config.py` — `config_root()`, `Defaults.project`, `Override.project`, loaders
- `src/clockify_horas/cli.py` — `add` lê `project_name` + grava histórico; `config set/add-override --project`; novo `suggest`
- `tests/test_entries.py`, `tests/test_cli.py`, `tests/test_config.py`, `tests/test_config_subcommand.py`
- `commands/horas.md`, `commands/lancar.md`, `skills/clockify-setup/SKILL.md`
- `README.md`, `MAINTAINER.md`, versão (lockstep 1.1.0)

(Prefixo comum dos paths de código: `plugins/clockify-horas/`.)

---

## PHASE 1 — Núcleo: resolução por (projeto, tarefa)

### Task 1: `TimeEntry.project_name` + `_resolve_task` project-aware

**Files:**
- Modify: `plugins/clockify-horas/src/clockify_horas/models.py`
- Modify: `plugins/clockify-horas/src/clockify_horas/entries.py`
- Test: `plugins/clockify-horas/tests/test_entries.py`

- [ ] **Step 1: Write the failing tests**

Adicione a `tests/test_entries.py` (use os imports já existentes do arquivo; se faltar, adicione `from datetime import datetime`, `from zoneinfo import ZoneInfo`, `from clockify_horas.entries import build_payload`, `from clockify_horas.models import Metadata, TimeEntry`):

```python
def _md_dup():
    return Metadata(
        workspace_id="w",
        user_id="u",
        projects={"Proj A": "p1", "Proj B": "p2"},
        tasks={("p1", "Dup"): "t1", ("p2", "Dup"): "t2", ("p1", "Único"): "t3"},
        tags={"Tag": "g1"},
    )


def _entry(task_name, project_name=None):
    tz = ZoneInfo("America/Sao_Paulo")
    return TimeEntry(
        description="x",
        start=datetime(2026, 6, 4, 9, 0, tzinfo=tz),
        end=datetime(2026, 6, 4, 10, 0, tzinfo=tz),
        task_name=task_name,
        tag_names=["Tag"],
        billable=False,
        project_name=project_name,
    )


def test_resolve_com_projeto_desambigua():
    p = build_payload(_entry("Dup", project_name="Proj B"), _md_dup())
    assert p["projectId"] == "p2"
    assert p["taskId"] == "t2"


def test_resolve_projeto_inexistente_levanta():
    try:
        build_payload(_entry("Dup", project_name="Proj X"), _md_dup())
    except KeyError as e:
        assert "Projeto não encontrado" in str(e)
    else:
        raise AssertionError("esperava KeyError")


def test_resolve_tarefa_fora_do_projeto_levanta():
    try:
        build_payload(_entry("Único", project_name="Proj B"), _md_dup())
    except KeyError as e:
        assert "não existe no projeto" in str(e)
    else:
        raise AssertionError("esperava KeyError")


def test_resolve_nome_unico_sem_projeto_ok():
    p = build_payload(_entry("Único"), _md_dup())
    assert p["projectId"] == "p1"
    assert p["taskId"] == "t3"


def test_resolve_ambiguo_sem_projeto_levanta():
    try:
        build_payload(_entry("Dup"), _md_dup())
    except KeyError as e:
        assert "ambígua" in str(e)
    else:
        raise AssertionError("esperava KeyError")
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd plugins/clockify-horas && uv run pytest tests/test_entries.py -k resolve -v`
Expected: FAIL — `TimeEntry` ainda não aceita `project_name`.

- [ ] **Step 3: Implement**

Em `models.py`, adicione o campo opcional ao final de `TimeEntry`:

```python
@dataclass
class TimeEntry:
    """Um lançamento de tempo a ser criado no Clockify (horários em hora local)."""

    description: str
    start: datetime
    end: datetime
    task_name: str
    tag_names: list[str]
    billable: bool
    project_name: str | None = None
```

Em `entries.py`, substitua `_resolve_task` e ajuste `build_payload`:

```python
def build_payload(entry: TimeEntry, metadata: Metadata) -> dict:
    """Resolve nomes -> IDs e monta o corpo do POST de time-entry do Clockify.

    Levanta KeyError com o nome ofensor se a tarefa/projeto/tag não resolver.
    """
    project_id, task_id = _resolve_task(entry.task_name, metadata, entry.project_name)
    tag_ids = [_resolve_tag(name, metadata) for name in entry.tag_names]
    return {
        "start": to_utc_iso(entry.start),
        "end": to_utc_iso(entry.end),
        "description": entry.description,
        "projectId": project_id,
        "taskId": task_id,
        "tagIds": tag_ids,
        "billable": entry.billable,
    }


def _resolve_task(
    task_name: str, metadata: Metadata, project_name: str | None = None
) -> tuple[str, str]:
    if project_name is not None:
        project_id = metadata.projects.get(project_name)
        if project_id is None:
            raise KeyError(f"Projeto não encontrado no Clockify: {project_name!r}")
        task_id = metadata.tasks.get((project_id, task_name))
        if task_id is None:
            raise KeyError(f"Tarefa {task_name!r} não existe no projeto {project_name!r}.")
        return project_id, task_id

    matches = [(pid, tid) for (pid, name), tid in metadata.tasks.items() if name == task_name]
    if not matches:
        raise KeyError(f"Tarefa não encontrada no Clockify: {task_name!r}")
    if len(matches) > 1:
        proj_by_id = {pid: pname for pname, pid in metadata.projects.items()}
        projs = ", ".join(proj_by_id.get(pid, pid) for pid, _ in matches)
        raise KeyError(
            f"Tarefa {task_name!r} ambígua: existe em múltiplos projetos ({projs}). "
            "Informe o projeto (campo project_name / --project) para desambiguar."
        )
    return matches[0]
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd plugins/clockify-horas && uv run pytest tests/test_entries.py -v`
Expected: PASS (novos + existentes).

- [ ] **Step 5: Commit**

```bash
git add plugins/clockify-horas/src/clockify_horas/models.py plugins/clockify-horas/src/clockify_horas/entries.py plugins/clockify-horas/tests/test_entries.py
git commit -m "feat(entries): resolver tarefa por (projeto, tarefa) com fallback por nome único"
```

---

### Task 2: `add` lê `project_name` do JSON

**Files:**
- Modify: `plugins/clockify-horas/src/clockify_horas/cli.py`
- Test: `plugins/clockify-horas/tests/test_cli.py`

- [ ] **Step 1: Write the failing test**

Adicione a `tests/test_cli.py` (no padrão respx/monkeypatch já usado no arquivo; `BASE = "https://api.clockify.me/api/v1"` já existe lá):

```python
@respx.mock
def test_add_dry_run_usa_project_name(monkeypatch, tmp_path, capsys):
    monkeypatch.setenv("XDG_CONFIG_HOME", str(tmp_path))
    monkeypatch.setenv("CLOCKIFY_API_KEY", "k")
    monkeypatch.setenv("CLOCKIFY_WORKSPACE_ID", "W")
    monkeypatch.setenv("OUTLOOK_ICS_URL", "")
    respx.get(f"{BASE}/user").mock(return_value=httpx.Response(200, json={"id": "u"}))
    respx.get(f"{BASE}/workspaces/W/projects").mock(
        return_value=httpx.Response(
            200, json=[{"id": "p1", "name": "Proj A"}, {"id": "p2", "name": "Proj B"}]
        )
    )
    respx.get(f"{BASE}/workspaces/W/projects/p1/tasks").mock(
        return_value=httpx.Response(200, json=[{"id": "t1", "name": "Dup"}])
    )
    respx.get(f"{BASE}/workspaces/W/projects/p2/tasks").mock(
        return_value=httpx.Response(200, json=[{"id": "t2", "name": "Dup"}])
    )
    respx.get(f"{BASE}/workspaces/W/tags").mock(
        return_value=httpx.Response(200, json=[{"id": "g1", "name": "Tag"}])
    )
    item = [
        {
            "description": "x",
            "start": "2026-06-04T09:00:00",
            "end": "2026-06-04T10:00:00",
            "task_name": "Dup",
            "tag_names": ["Tag"],
            "billable": False,
            "project_name": "Proj B",
        }
    ]
    f = tmp_path / "e.json"
    f.write_text(json.dumps(item), encoding="utf-8")
    rc = main(["add", "--file", str(f), "--dry-run"])
    assert rc == 0
    payloads = json.loads(capsys.readouterr().out)
    assert payloads[0]["projectId"] == "p2"
    assert payloads[0]["taskId"] == "t2"
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd plugins/clockify-horas && uv run pytest tests/test_cli.py::test_add_dry_run_usa_project_name -v`
Expected: FAIL — `project_name` é ignorado; `Dup` resolve como ambíguo (KeyError) e retorna 1.

- [ ] **Step 3: Implement**

Em `cli.py`, na construção dos `TimeEntry` dentro de `_cmd_add`, acrescente `project_name`:

```python
            TimeEntry(
                description=item["description"],
                start=_parse_local(item["start"]),
                end=_parse_local(item["end"]),
                task_name=item["task_name"],
                tag_names=item["tag_names"],
                billable=bool(item["billable"]),
                project_name=item.get("project_name"),
            )
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd plugins/clockify-horas && uv run pytest tests/test_cli.py -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add plugins/clockify-horas/src/clockify_horas/cli.py plugins/clockify-horas/tests/test_cli.py
git commit -m "feat(cli): add lê project_name opcional por item"
```

---

## PHASE 2 — Config qualificado por projeto

### Task 3: `project` em `Defaults`/`Override` + `from_event`

**Files:**
- Modify: `plugins/clockify-horas/src/clockify_horas/config.py`
- Modify: `plugins/clockify-horas/src/clockify_horas/entries.py`
- Test: `plugins/clockify-horas/tests/test_config.py`, `plugins/clockify-horas/tests/test_entries.py`

- [ ] **Step 1: Write the failing tests**

Em `tests/test_config.py` (imports já trazem `Defaults`; adicione `Override` ao import de `clockify_horas.config` onde necessário):

```python
def test_load_defaults_com_projeto(monkeypatch, tmp_path):
    monkeypatch.setenv("XDG_CONFIG_HOME", str(tmp_path))
    from clockify_horas.config import load_defaults, write_raw

    write_raw(
        {
            "defaults": {
                "task_name": "T",
                "tag_name": "G",
                "billable": False,
                "daily_target_hours": 8.0,
                "project": "Proj A",
            }
        }
    )
    assert load_defaults().project == "Proj A"


def test_load_defaults_sem_projeto_none(monkeypatch, tmp_path):
    monkeypatch.setenv("XDG_CONFIG_HOME", str(tmp_path))
    from clockify_horas.config import load_defaults, write_raw

    write_raw(
        {"defaults": {"task_name": "T", "tag_name": "G", "billable": False, "daily_target_hours": 8.0}}
    )
    assert load_defaults().project is None


def test_load_overrides_com_projeto(monkeypatch, tmp_path):
    monkeypatch.setenv("XDG_CONFIG_HOME", str(tmp_path))
    from clockify_horas.config import load_overrides, write_raw

    write_raw(
        {"overrides": [{"match": "M", "task_name": "T", "tag_name": "G", "billable": True, "project": "Proj B"}]}
    )
    ov = load_overrides()
    assert ov[0].project == "Proj B"
```

Em `tests/test_entries.py`:

```python
def test_from_event_propaga_projeto_do_default():
    from datetime import datetime
    from zoneinfo import ZoneInfo

    from clockify_horas.config import Defaults
    from clockify_horas.entries import from_event
    from clockify_horas.models import CalEvent

    tz = ZoneInfo("America/Sao_Paulo")
    ev = CalEvent(title="x", start=datetime(2026, 6, 4, 9, tzinfo=tz), end=datetime(2026, 6, 4, 10, tzinfo=tz))
    d = Defaults(task_name="T", tag_name="G", billable=False, daily_target_hours=8.0, project="Proj A")
    assert from_event(ev, d).project_name == "Proj A"
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd plugins/clockify-horas && uv run pytest tests/test_config.py -k projeto tests/test_entries.py::test_from_event_propaga_projeto_do_default -v`
Expected: FAIL — `Defaults`/`Override` não têm `project`; `from_event` não propaga.

- [ ] **Step 3: Implement**

Em `config.py`, adicione `project` (opcional) às dataclasses e aos loaders:

```python
@dataclass
class Defaults:
    task_name: str
    tag_name: str
    billable: bool
    daily_target_hours: float
    project: str | None = None


@dataclass
class Override:
    match: str
    task_name: str
    tag_name: str
    billable: bool
    project: str | None = None
```

Em `load_defaults`, dentro do `Defaults(...)`, acrescente `project=d.get("project")`. Em `load_overrides`, dentro do `Override(...)`, acrescente `project=o.get("project")`.

Em `entries.py`, ajuste `from_event`:

```python
def from_event(event: CalEvent, defaults: Defaults) -> TimeEntry:
    """Converte uma reunião do calendário num lançamento aplicando os defaults."""
    return TimeEntry(
        description=event.title,
        start=event.start,
        end=event.end,
        task_name=defaults.task_name,
        tag_names=[defaults.tag_name],
        billable=defaults.billable,
        project_name=defaults.project,
    )
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd plugins/clockify-horas && uv run pytest tests/test_config.py tests/test_entries.py -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add plugins/clockify-horas/src/clockify_horas/config.py plugins/clockify-horas/src/clockify_horas/entries.py plugins/clockify-horas/tests/test_config.py plugins/clockify-horas/tests/test_entries.py
git commit -m "feat(config): campo project opcional em Defaults/Override + from_event"
```

---

### Task 4: `config set --project` e `config add-override --project`

**Files:**
- Modify: `plugins/clockify-horas/src/clockify_horas/cli.py`
- Test: `plugins/clockify-horas/tests/test_config_subcommand.py`

- [ ] **Step 1: Write the failing tests**

Adicione a `tests/test_config_subcommand.py`:

```python
def test_config_set_project(monkeypatch, tmp_path):
    monkeypatch.setenv("XDG_CONFIG_HOME", str(tmp_path))
    rc = main(["config", "set", "--task", "T", "--project", "Proj A"])
    assert rc == 0
    data = json.loads(config_path().read_text(encoding="utf-8"))
    assert data["defaults"]["project"] == "Proj A"


def test_config_add_override_project(monkeypatch, tmp_path):
    monkeypatch.setenv("XDG_CONFIG_HOME", str(tmp_path))
    rc = main(
        ["config", "add-override", "--match", "M", "--task", "T", "--tag", "G",
         "--billable", "--project", "Proj B"]
    )
    assert rc == 0
    data = json.loads(config_path().read_text(encoding="utf-8"))
    assert data["overrides"][0]["project"] == "Proj B"
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd plugins/clockify-horas && uv run pytest tests/test_config_subcommand.py -k project -v`
Expected: FAIL — argparse: `unrecognized arguments: --project`.

- [ ] **Step 3: Implement**

Em `_cmd_config_set`, antes do `write_raw(data)`, acrescente:

```python
    if args.project is not None:
        df["project"] = args.project
```

Em `_cmd_config_add_override`, monte a entrada incluindo `project` só quando informado:

```python
def _cmd_config_add_override(args: argparse.Namespace) -> int:
    data = read_raw()
    ov = data.setdefault("overrides", [])
    entry = {
        "match": args.match,
        "task_name": args.task,
        "tag_name": args.tag,
        "billable": bool(args.billable) if args.billable is not None else False,
    }
    if args.project is not None:
        entry["project"] = args.project
    ov.append(entry)
    p = write_raw(data)
    print(f"Override adicionado ({args.match}): {p}")
    return 0
```

Em `build_parser`: adicione `p_set.add_argument("--project")` (junto aos outros flags de `set`) e `p_ovr.add_argument("--project")` (no parser de `add-override`).

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd plugins/clockify-horas && uv run pytest tests/test_config_subcommand.py -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add plugins/clockify-horas/src/clockify_horas/cli.py plugins/clockify-horas/tests/test_config_subcommand.py
git commit -m "feat(cli): config set --project e add-override --project"
```

---

## PHASE 3 — Histórico + sugestão

### Task 5: módulo `history.py`

**Files:**
- Modify: `plugins/clockify-horas/src/clockify_horas/config.py` (extrair `config_root`)
- Create: `plugins/clockify-horas/src/clockify_horas/history.py`
- Test: `plugins/clockify-horas/tests/test_history.py`

- [ ] **Step 1: Write the failing tests**

Crie `tests/test_history.py`:

```python
import os
import stat


def test_history_path_respeita_xdg(monkeypatch, tmp_path):
    monkeypatch.setenv("XDG_CONFIG_HOME", str(tmp_path))
    from clockify_horas.history import history_path

    assert history_path() == tmp_path / "clockify-horas" / "history.json"


def test_record_e_suggest_roundtrip(monkeypatch, tmp_path):
    monkeypatch.setenv("XDG_CONFIG_HOME", str(tmp_path))
    from clockify_horas.history import history_path, record_entry, suggest_for

    record_entry("Daily da Equipe", "Time IA", ["Tag"], False, "Proj A")
    s = suggest_for("  daily da equipe  ")  # normaliza trim/lowercase
    assert s == {
        "project_name": "Proj A",
        "task_name": "Time IA",
        "tag_names": ["Tag"],
        "billable": False,
    }
    if os.name == "posix":
        assert stat.S_IMODE(history_path().stat().st_mode) == 0o600


def test_suggest_miss_none(monkeypatch, tmp_path):
    monkeypatch.setenv("XDG_CONFIG_HOME", str(tmp_path))
    from clockify_horas.history import suggest_for

    assert suggest_for("nada") is None


def test_record_upsert_sobrescreve(monkeypatch, tmp_path):
    monkeypatch.setenv("XDG_CONFIG_HOME", str(tmp_path))
    from clockify_horas.history import record_entry, suggest_for

    record_entry("X", "T1", ["G"], False, None)
    record_entry("X", "T2", ["G"], True, "Proj B")
    assert suggest_for("X") == {
        "project_name": "Proj B",
        "task_name": "T2",
        "tag_names": ["G"],
        "billable": True,
    }


def test_read_history_corrompido_vazio(monkeypatch, tmp_path):
    monkeypatch.setenv("XDG_CONFIG_HOME", str(tmp_path))
    from clockify_horas.history import history_path, read_history

    p = history_path()
    p.parent.mkdir(parents=True, exist_ok=True)
    p.write_text("{ não é json", encoding="utf-8")
    assert read_history() == {}
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd plugins/clockify-horas && uv run pytest tests/test_history.py -v`
Expected: FAIL — módulo `history` inexistente.

- [ ] **Step 3: Implement**

Em `config.py`, extraia a raiz para reúso (substitua `config_path`):

```python
def config_root() -> Path:
    """Diretório da config por SO. ``$XDG_CONFIG_HOME`` tem prioridade em qualquer SO."""
    base = os.getenv("XDG_CONFIG_HOME")
    if base:
        return Path(base) / APP_DIR
    if os.name == "nt" and os.getenv("APPDATA"):
        return Path(os.environ["APPDATA"]) / APP_DIR
    return Path.home() / ".config" / APP_DIR


def config_path() -> Path:
    return config_root() / "config.json"
```

Crie `src/clockify_horas/history.py`:

```python
import json
import os
from pathlib import Path

from clockify_horas.config import config_root


def history_path() -> Path:
    return config_root() / "history.json"


def _normalize(description: str) -> str:
    return description.strip().lower()


def read_history(path: Path | None = None) -> dict:
    p = path or history_path()
    if not p.exists():
        return {}
    try:
        return json.loads(p.read_text(encoding="utf-8"))
    except (OSError, json.JSONDecodeError):
        return {}


def write_history(data: dict, path: Path | None = None) -> Path:
    p = path or history_path()
    p.parent.mkdir(parents=True, exist_ok=True)
    p.write_text(json.dumps(data, ensure_ascii=False, indent=2) + "\n", encoding="utf-8")
    if os.name == "posix":
        p.chmod(0o600)
    return p


def record_entry(
    description: str,
    task_name: str,
    tag_names: list[str],
    billable: bool,
    project_name: str | None,
    path: Path | None = None,
) -> None:
    data = read_history(path)
    # chaves espelham o item do JSON do `add` (project_name/task_name/tag_names/billable),
    # para a sugestão mapear direto sem renomear campo.
    data[_normalize(description)] = {
        "project_name": project_name,
        "task_name": task_name,
        "tag_names": list(tag_names),
        "billable": bool(billable),
    }
    write_history(data, path)


def suggest_for(description: str, path: Path | None = None) -> dict | None:
    return read_history(path).get(_normalize(description))
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd plugins/clockify-horas && uv run pytest tests/test_history.py tests/test_config.py -v`
Expected: PASS (history + config_path inalterado).

- [ ] **Step 5: Commit**

```bash
git add plugins/clockify-horas/src/clockify_horas/config.py plugins/clockify-horas/src/clockify_horas/history.py plugins/clockify-horas/tests/test_history.py
git commit -m "feat(history): módulo de histórico local (record/suggest) + config_root"
```

---

### Task 6: `add` grava histórico no sucesso

**Files:**
- Modify: `plugins/clockify-horas/src/clockify_horas/cli.py`
- Test: `plugins/clockify-horas/tests/test_cli.py`

- [ ] **Step 1: Write the failing test**

Adicione a `tests/test_cli.py` (real add — mocka o POST):

```python
@respx.mock
def test_add_grava_historico_no_sucesso(monkeypatch, tmp_path):
    monkeypatch.setenv("XDG_CONFIG_HOME", str(tmp_path))
    monkeypatch.setenv("CLOCKIFY_API_KEY", "k")
    monkeypatch.setenv("CLOCKIFY_WORKSPACE_ID", "W")
    monkeypatch.setenv("OUTLOOK_ICS_URL", "")
    respx.get(f"{BASE}/user").mock(return_value=httpx.Response(200, json={"id": "u"}))
    respx.get(f"{BASE}/workspaces/W/projects").mock(
        return_value=httpx.Response(200, json=[{"id": "p1", "name": "Proj A"}])
    )
    respx.get(f"{BASE}/workspaces/W/projects/p1/tasks").mock(
        return_value=httpx.Response(200, json=[{"id": "t1", "name": "T"}])
    )
    respx.get(f"{BASE}/workspaces/W/tags").mock(
        return_value=httpx.Response(200, json=[{"id": "g1", "name": "G"}])
    )
    respx.post(f"{BASE}/workspaces/W/time-entries").mock(
        return_value=httpx.Response(201, json={"id": "e1"})
    )
    item = [
        {
            "description": "Reunião Recorrente",
            "start": "2026-06-04T09:00:00",
            "end": "2026-06-04T10:00:00",
            "task_name": "T",
            "tag_names": ["G"],
            "billable": False,
            "project_name": "Proj A",
        }
    ]
    f = tmp_path / "e.json"
    f.write_text(json.dumps(item), encoding="utf-8")
    rc = main(["add", "--file", str(f)])
    assert rc == 0
    from clockify_horas.history import suggest_for

    assert suggest_for("Reunião Recorrente") == {
        "project_name": "Proj A",
        "task_name": "T",
        "tag_names": ["G"],
        "billable": False,
    }
```

Também garanta que o **dry-run NÃO grava**: adicione um assert ao final de `test_add_dry_run_usa_project_name` (Task 2):

```python
    from clockify_horas.history import suggest_for

    assert suggest_for("x") is None  # dry-run não grava histórico
```

- [ ] **Step 1b: Isolar os testes de `add` que fazem POST real (CRÍTICO — senão poluem o `~/.config` real)**

A partir da Task 6, qualquer `add` bem-sucedido grava `history.json` em `config_root()`. Os dois
testes pré-existentes que fazem POST real usam o helper `_setup_env(monkeypatch)` (em
`tests/test_cli.py`), que **NÃO** seta `XDG_CONFIG_HOME` — então passariam a escrever no
`~/.config/clockify-horas/history.json` real do dev. Ambos já recebem `tmp_path` na assinatura.
Adicione `monkeypatch.setenv("XDG_CONFIG_HOME", str(tmp_path))` no início de cada um:

- `test_add_real_posta` (≈ `tests/test_cli.py:67`) — confirmar que a assinatura tem `tmp_path`
  (adicionar o param se faltar) e acrescentar o `setenv` de XDG.
- `test_add_para_limpo_na_falha_parcial` (≈ `tests/test_cli.py:215`) — idem (o 1º item é criado
  com sucesso antes da falha, então também gravaria histórico).

Rode `cd plugins/clockify-horas && uv run pytest tests/test_cli.py -q` e confirme que continuam
verdes. Confirme manualmente que **nenhum** `history.json` apareceu fora do `tmp_path` (ex.:
`ls ~/.config/clockify-horas/history.json` não deve existir por causa dos testes).

- [ ] **Step 2: Run test to verify it fails**

Run: `cd plugins/clockify-horas && uv run pytest tests/test_cli.py::test_add_grava_historico_no_sucesso -v`
Expected: FAIL — histórico não é gravado (suggest_for retorna None).

- [ ] **Step 3: Implement**

Em `cli.py`, importe o módulo: `from clockify_horas import history`. No loop de gravação do `_cmd_add`, itere sobre `entries` junto com `payloads` e grave o histórico após cada sucesso:

```python
    gravados: list[Any] = []
    for i, (entry, p) in enumerate(zip(entries, payloads), start=1):
        try:
            resp = client.create_entry(p)
        except httpx.HTTPError as e:
            desc = p["description"]
            print(
                f"FALHA no item {i}/{len(payloads)} ({desc} @ {p['start']}): {e}", file=sys.stderr
            )
            print(
                f"Gravados com sucesso antes da falha: {len(gravados)}/{len(payloads)}. "
                f"Reexecute apenas os itens restantes para evitar duplicata.",
                file=sys.stderr,
            )
            return 1
        gravados.append(resp.get("id"))
        history.record_entry(
            entry.description, entry.task_name, entry.tag_names, entry.billable, entry.project_name
        )
        print(f"Lançado: {p['description']} -> {resp.get('id')}")
    return 0
```

(O `--dry-run` retorna antes deste loop, então não grava.)

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd plugins/clockify-horas && uv run pytest tests/test_cli.py -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add plugins/clockify-horas/src/clockify_horas/cli.py plugins/clockify-horas/tests/test_cli.py
git commit -m "feat(cli): add grava histórico (descrição->projeto/tarefa) no sucesso"
```

---

### Task 7: subcomando `suggest`

**Files:**
- Modify: `plugins/clockify-horas/src/clockify_horas/cli.py`
- Test: `plugins/clockify-horas/tests/test_config_subcommand.py`

- [ ] **Step 1: Write the failing test**

Adicione a `tests/test_config_subcommand.py`:

```python
def test_suggest_match_e_miss(monkeypatch, tmp_path, capsys):
    monkeypatch.setenv("XDG_CONFIG_HOME", str(tmp_path))
    from clockify_horas.history import record_entry

    record_entry("Daily", "Time IA", ["Tag"], False, "Proj A")
    rc = main(["suggest", "--description", "daily"])
    assert rc == 0
    assert json.loads(capsys.readouterr().out) == {
        "project_name": "Proj A",
        "task_name": "Time IA",
        "tag_names": ["Tag"],
        "billable": False,
    }
    rc = main(["suggest", "--description", "outra"])
    assert rc == 0
    assert json.loads(capsys.readouterr().out) == {}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd plugins/clockify-horas && uv run pytest tests/test_config_subcommand.py::test_suggest_match_e_miss -v`
Expected: FAIL — `invalid choice: 'suggest'`.

- [ ] **Step 3: Implement**

Em `cli.py`, adicione a função:

```python
def _cmd_suggest(args: argparse.Namespace) -> int:
    s = history.suggest_for(args.description)
    print(json.dumps(s or {}, ensure_ascii=False, indent=2))
    return 0
```

Em `build_parser`, registre o subcomando (perto de `add`):

```python
    p_sug = sub.add_parser("suggest", help="Sugere projeto/tarefa do histórico por descrição")
    p_sug.add_argument("--description", required=True, help="Descrição do evento/atividade")
    p_sug.set_defaults(func=_cmd_suggest)
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd plugins/clockify-horas && uv run pytest tests/test_config_subcommand.py -v`
Expected: PASS.

- [ ] **Step 5: Full suite + lint + types**

Run: `cd plugins/clockify-horas && uv run pytest -q && uv run ruff check . && uv run pyright`
Expected: tudo verde.

- [ ] **Step 6: Commit**

```bash
git add plugins/clockify-horas/src/clockify_horas/cli.py plugins/clockify-horas/tests/test_config_subcommand.py
git commit -m "feat(cli): subcomando suggest (lê o histórico por descrição)"
```

---

## PHASE 4 — Conversa + skill

### Task 8: generalizar `/horas`, `/lancar`, `/clockify-setup`

**Files:**
- Modify: `plugins/clockify-horas/commands/horas.md`, `commands/lancar.md`, `skills/clockify-setup/SKILL.md`

> Sem teste automatizado (são prompts). Validação no dogfood.

- [ ] **Step 1: `commands/horas.md`** — integrar a consulta ao histórico e a precedência na estrutura ATUAL do prompt (não criar passo novo).

O `horas.md` atual aplica os `defaults` **dentro do passo 1** ("Ler a agenda") e trata
overrides **no passo 4** ("Overrides + edição colaborativa"). Faça dois ajustes cirúrgicos:

(a) **No passo 1**, onde hoje diz "aplique os `defaults` da config", troque por: para CADA
evento, rode `clockify-horas suggest --description "<título do evento>"` e monte o candidato
pela **precedência: (1) override** cujo `match` aparece na descrição (resolvido no passo 4),
**(2) sugestão do histórico** (se `suggest` retornar algo não-vazio, use seus campos e diga
"da última vez foi projeto X / tarefa Y — mantenho?"), **(3) defaults**.

(b) **No passo 4**, acrescente que o override declarado tem prioridade sobre a sugestão do
histórico, e que a pessoa pode trocar o projeto de qualquer item ("esse é do projeto Z").

> A saída do `suggest` já vem com as chaves do item do `add` (`project_name`/`task_name`/
> `tag_names`/`billable`) — **use-as direto** ao montar o JSON, sem renomear (`tag_names` é
> lista; `suggest` já devolve lista).

No passo "Confirmação + dry-run", substituir o bloco JSON de exemplo por um que inclui `project_name`:

```markdown
   ```json
   [
     {
       "description": "Daily da equipe",
       "start": "2026-06-04T09:00:00",
       "end": "2026-06-04T10:00:00",
       "task_name": "<task_name do default/override/histórico>",
       "tag_names": ["<tag_name>"],
       "billable": false,
       "project_name": "<projeto, se a tarefa não for de nome único; senão omita>"
     }
   ]
   ```
```

E acrescentar uma linha: "Inclua `project_name` quando a tarefa existir em mais de um projeto (a maioria, em workspaces de consultoria); omita se o nome da tarefa for único."

- [ ] **Step 2: `commands/lancar.md`** — mesma lógica. No passo 4 (ditar atividades), acrescentar a consulta ao histórico e a precedência (mesmo parágrafo do horas.md, adaptado a "atividade ditada / evento do dia"). No passo 6 (Dry-run), trocar o schema JSON pelo mesmo bloco com `project_name` acima, e a mesma linha sobre quando incluí-lo.

- [ ] **Step 3: `skills/clockify-setup/SKILL.md`** — no passo 4 (Defaults), acrescentar projeto opcional ao default:

```markdown
   - (Opcional) projeto do default, se a tarefa default existir em mais de um projeto →
     `--project "<nome do projeto>"`. Se a tarefa default for de nome único (ex.: "Time IA"),
     pode pular.
   Grave: `clockify-horas config set --task "..." --tag "..." --no-billable --daily-target 8` (acrescente `--project "..."` se aplicável).
```

- [ ] **Step 4: Verificar ausência de dado pessoal + YAML válido**

Run: `cd plugins/clockify-horas && grep -rniE "time ia|célula de inovação|san pablo" commands/ skills/ ; echo "exit $?"`
Expected: exit 1 (nada). E `claude plugin validate plugins/clockify-horas` (da raiz) deve passar.

- [ ] **Step 5: Commit**

```bash
git add plugins/clockify-horas/commands plugins/clockify-horas/skills
git commit -m "docs(plugin): /horas e /lancar consultam suggest + precedência; project_name no schema; setup com projeto opcional"
```

---

## PHASE 5 — Release 1.1.0

### Task 9: bump 1.1.0 (lockstep) + docs + validação

**Files:**
- Modify: `plugins/clockify-horas/.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `plugins/clockify-horas/pyproject.toml`, `plugins/clockify-horas/src/clockify_horas/__init__.py`, `plugins/clockify-horas/tests/test_smoke.py`, `README.md`, `MAINTAINER.md`

- [ ] **Step 1: Bump em lockstep 1.0.1 → 1.1.0**

```bash
cd "<repo-root>"
python3 -c "
files=['.claude-plugin/marketplace.json','plugins/clockify-horas/.claude-plugin/plugin.json','plugins/clockify-horas/pyproject.toml','plugins/clockify-horas/src/clockify_horas/__init__.py','plugins/clockify-horas/tests/test_smoke.py']
for f in files:
    s=open(f,encoding='utf-8').read().replace('1.0.1','1.1.0')
    open(f,'w',encoding='utf-8').write(s); print('bumped',f)
"
```

- [ ] **Step 2: README — adicionar `suggest` e nota do histórico**

Em `README.md`, na seção "CLI direta", após `clockify-horas workspaces`, acrescente `clockify-horas suggest --description "..."`. E na seção de Config, uma linha: "O histórico de mapeamentos (descrição → projeto/tarefa) fica em `~/.config/clockify-horas/history.json` (ou `%APPDATA%`), só local; o `add` o atualiza e o `/horas`/`/lancar` o usam para sugerir."

- [ ] **Step 3: MAINTAINER — corrigir a lista de lockstep para 5 pontos + nota da v1.1**

Em `MAINTAINER.md`, o passo de release hoje enumera **4** arquivos de versão, mas o release
também fixa a versão em `tests/test_smoke.py` (asserção `__version__`). Atualize a lista do
lockstep para os **5 pontos**: `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`,
`pyproject.toml`, `src/clockify_horas/__init__.py` e `tests/test_smoke.py`. Acrescente a nota:
"v1.1 adiciona `history.json` (por-usuário, não versionado) e o subcomando `suggest`."

- [ ] **Step 4: Verificação final**

Run:
```bash
cd plugins/clockify-horas && uv run pytest -q && uv run ruff check . && uv run pyright
cd "<repo-root>" && claude plugin validate . && claude plugin validate plugins/clockify-horas
grep -rniE "time ia|célula de inovação|san pablo" plugins/ ; echo "exit $? (1=limpo)"
```
Expected: suíte verde; ambos `validate` ✔; grep exit 1.

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "chore: release 1.1.0 (resolução por projeto + histórico)"
```

---

## PHASE 6 — Dogfood (manual)

### Task 10: validar o fluxo real

> Manual — após reinstalar (`uv tool install --force plugins/clockify-horas` ou reabrir a sessão p/ o hook). Marque ao confirmar.

- [ ] **Step 1:** `clockify-horas config set --project "<um projeto real>"` (ou deixe sem) e `clockify-horas suggest --description "qualquer"` → `{}` (histórico vazio).
- [ ] **Step 2:** `/horas <hoje>` até o **dry-run**: confirmar que um evento de projeto não-default pode receber `project_name` e o payload resolve o `projectId`/`taskId` certos. NÃO gravar (ou gravar 1 item de teste e conferir que `suggest` passa a retorná-lo).
- [ ] **Step 3:** Repetir `/horas` e confirmar que a sugestão do histórico aparece para o evento recorrente.
- [ ] **Step 4 (verification-before-completion):** suíte verde + `git status` limpo. `superpowers:finishing-a-development-branch` para integrar.

---

## Self-Review (preenchido na escrita)

- **Cobertura do spec:** resolução (Task 1), add lê project_name (Task 2), Defaults/Override.project + from_event (Task 3), config set/add-override --project (Task 4), history.py (Task 5), add grava (Task 6), suggest (Task 7), conversa+skill (Task 8), release (Task 9), dogfood (Task 10). Sem requisito órfão.
- **Retrocompat:** todos os campos `project`/`project_name` são opcionais (default None); loaders usam `.get("project")`; itens do `add` usam `.get("project_name")`. Config v1.0 e o `Time IA` único do autor seguem funcionando.
- **Consistência de tipos:** `project`/`project_name: str | None`; `_resolve_task(task_name, metadata, project_name=None)`; `history.record_entry(description, task_name, tag_names, billable, project_name, path=None)`; `suggest_for(description, path=None) -> dict | None`; schema do histórico `{project_name, task_name, tag_names, billable}` — **mesmas chaves do item do `add`** — idêntico entre `record_entry`, `suggest_for`, testes e o `suggest`.
- **Isolamento de testes:** todo teste que escreve config OU histórico seta `XDG_CONFIG_HOME=tmp_path`. A **Task 6 Step 1b** isola explicitamente os dois testes pré-existentes de POST real (`test_add_real_posta`, `test_add_para_limpo_na_falha_parcial`) para não tocar `~/.config`.
- **Precedência:** override > histórico > default — implementada na camada de conversa (prompts), não no motor (que só resolve nomes→IDs).
- **`from_event`:** é helper hoje sem chamador em produção (a conversa monta o JSON direto). Propagar `defaults.project` mantém o helper coerente e testado, sem custo; não é caminho crítico.
- **Correções do plan-critic aplicadas:** C1 (Step 1b explícito isolando os 2 testes de POST real); W2 (chaves do histórico reconciliadas com o item do `add` — sem remapeamento no prompt); W1 (Task 8 ancorada na estrutura real de `horas.md`/`lancar.md`); W3 (MAINTAINER passa a listar 5 pontos de versão).
