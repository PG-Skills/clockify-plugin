# Clockify v1.2 — Simplicidade para leigos (onboarding + default opcional + atividades aprendidas) — Design

**Data:** 2026-06-04
**Autor:** vbjuliani (PG Consulting)
**Status:** Aprovado (design) — pendente plano de implementação
**Relacionado:** evolui [[2026-06-04-clockify-v1.1-projeto-qualificado-design]]. Versão permanece **1.0.0** (política pré-lançamento — ver `MAINTAINER.md`).

## Princípio mestre (inviolável)

**O plugin é para usuários leigos (não-técnicos) e deve ser super simples.** Nada de "coisa
avançada" exposta. O Claude faz o trabalho; o usuário só confirma. Esconder todo o encanamento
(`project_name`, JSON, flags, "override", "histórico", "dry-run", "workspace-id"). Em qualquer
trade-off, escolher a simplicidade (YAGNI agressivo). Ver memória `clockify-simplicidade-leigos`.

Esta release revisa o onboarding e a funcionalidade inteira sob essa lente. São **3 pilares**.

---

## Pilar 1 — Onboarding de 3 perguntas (`/clockify-setup`)

Reescrita do `skills/clockify-setup/SKILL.md`. Linguagem leiga, sem jargão, perguntas mínimas;
o que é específico é **pulável** e explicado em uma frase.

1. **Conectar ao Clockify.** "Cola sua chave do Clockify aqui (te mostro onde pegar: perfil →
   Preferences → Advanced → Generate)." Grava a key; lista workspaces — **se houver um, escolhe
   sozinho**; se vários, pergunta em lista simples.
2. **Agenda do Outlook (opcional, pulável).** "Quer que eu puxe sua agenda automática? Cola o
   link do calendário publicado (te explico como pegar). **Ou pula** — você me dita as horas e
   funciona igual." Grava o ICS ou segue sem.
3. **Atividade padrão (opcional).** Pergunta clara com explicação: "Quer configurar uma
   **atividade padrão**? É a tarefa que entra na maioria dos seus lançamentos — útil se a maior
   parte do seu dia é uma coisa só (ex.: trabalho interno), porque você não reinforma toda vez.
   **Se você atua em vários projetos/clientes sem uma atividade dominante, pule** — aí eu aprendo
   suas atividades e pergunto quando precisar." Se **sim** → tarefa + etiqueta + "essas horas são
   faturáveis?" (+ projeto, só se a tarefa repetir entre projetos — invisível se única). Se
   **não** → não grava nada (meta diária fica 8h silenciosa).

Fim: roda a validação e diz em português comum — "Testei, tá tudo certo! ✅ Agora é só `/horas`
(lança o dia) ou `/lancar` (vários dias)."

**Removido do onboarding:** o passo de **overrides** (vira "atividade aprendida", Pilar 3) e a
pergunta de meta diária (silenciosa em 8h; ajustável depois). **ICS deixa de ser obrigatório.**

---

## Pilar 2 — Default opcional (código)

Hoje `load_defaults` **exige** tarefa/etiqueta/faturável (levanta `ValueError`), inviabilizando o
multi-projeto. Tornar o "trio" opcional.

- `config.py` `Defaults`: `task_name: str | None = None`, `tag_name: str | None = None`,
  `billable: bool | None = None`, `daily_target_hours: float = 8.0`, `project: str | None = None`.
- `load_defaults`: **não levanta** mais; lê com `.get`, `daily_target_hours` cai pra `8.0`.
  Config sem `defaults` carrega numa boa (tudo None + 8h).
- `config doctor`: quando não há atividade padrão, imprime algo neutro — ex.:
  "OK: sem atividade padrão (aprendo pelas suas atividades)" — em vez de "tarefa 'None' não
  encontrada".
- **Remover `from_event`** (`entries.py`) e seu teste: é código morto (verificar via grep que só
  o teste o chama; a conversa monta o JSON direto) e fica incoerente com default ausente
  (aplicaria `task_name=None`). Limpeza alinhada ao trabalho.

`add`/`_resolve_task`/resolução por projeto: **inalterados**.

---

## Pilar 3 — "Atividades aprendidas" (unifica histórico + override)

Para o usuário existe **uma coisa só**: as atividades que o plugin foi aprendendo. Internamente,
funde o `history.json` (exato) e os `overrides` (palavra-chave) da v1.1 numa **única memória**, e
**o reconhecimento passa a ser feito pelo Claude** (não por algoritmo no código).

### Dados — `~/.config/clockify-horas/learned.json` (por-usuário, local, nunca no repo)

Lista de atividades. Cada uma: `{match, project_name, task_name, tag_names, billable}`:
- `match`: **como reconhecer** — um título de reunião OU uma palavra-chave (ex.: "Mulesoft").
- `project_name`/`task_name`/`tag_names`/`billable`: **pra onde vai** (mesmas chaves do item do
  `add` — o Claude copia direto, sem renomear).

Substitui `history.json` **e** a seção `overrides` do config (e o subcomando `config add-override`).

### Como aprende (dois jeitos, ambos invisíveis e em português)

1. **Sozinho, ao lançar:** o `add`, após cada lançamento criado com sucesso, faz upsert de uma
   atividade com `match = descrição` (dedup por descrição). Recorrente de título igual já passa a
   ser reconhecido. (É o `record` da v1.1, agora na memória unificada.)
2. **Por padrão percebido (com consentimento):** quando o Claude nota repetição com termo em
   comum, pergunta em linguagem leiga ("Toda vez que aparecer 'Mulesoft', já lanço no projeto X
   faturável? sim/não") e, no "sim", grava uma atividade com `match = palavra-chave` via
   `clockify-horas learned add ...`. (Substitui o `config add-override`; o usuário nunca vê
   "override".)

### Como o Claude relaciona

No `/horas`/`/lancar`, o Claude roda `clockify-horas learned list` **uma vez**, lê as atividades
e, para cada evento do dia, reconhece a que combina — **título igual, parecido, ou palavra-chave**
— e propõe em tabela plain (ver exemplo abaixo). O que não reconhecer, **pergunta o projeto/tarefa
em português comum**. O código não tenta adivinhar; o Claude reconhece e o usuário confirma.

Exemplo da conversa do `/horas`:

```
Reunião                     | Vou lançar em                                  |
AI Innovation - Daily       | Time IA · Célula de Inovação · não-faturável    (você sempre lança assim)
Revisión Mulesoft           | Farmacia San Pablo · Assinatura · faturável     (aprendi do "Mulesoft")
Conversa com fornecedor     | ❓ não reconheci — de qual projeto/cliente é?
```

**Precedência ao montar cada item:** atividade aprendida reconhecida pelo Claude **>** atividade
padrão (default, se existir) **>** perguntar. Sempre termina em dry-run + confirmação (já existem).

### CLI (superfície do Pilar 3)

- `learned list` — imprime as atividades aprendidas (JSON) para o Claude ler.
- `learned add --match ... --project ... --task ... --tag ... --billable/--no-billable` — adiciona
  uma atividade por palavra-chave (chamado quando o Claude promove um padrão, com o "sim" do usuário).
- `add` (estendido) — grava/atualiza a atividade automaticamente no sucesso (não em `--dry-run`;
  falha parcial só grava os criados).
- **Removidos:** `suggest --description` (o Claude faz o reconhecimento agora) e
  `config add-override` (virou `learned add`). A seção `overrides` do config deixa de ser usada.

Escrita do `learned.json` com `chmod 600` em POSIX; arquivo corrompido/ausente tratado como lista
vazia (nunca quebra o `add`/`learned list`).

---

## Error handling

- Sem default → fluxo segue (Pilar 2); doctor neutro; `/horas` pergunta o que não reconhecer.
- `learned.json` ausente/corrompido → lista vazia.
- Resolução `(projeto, tarefa)`: erros claros da v1.1 mantidos (projeto/tarefa inexistente).
- Nenhuma mensagem técnica vaza pro usuário — o Claude traduz para linguagem comum.

## Testes

- `load_defaults` tolerante: sem `defaults`/parcial → campos None + `daily_target_hours=8.0`; não levanta.
- `config doctor` sem default → linha neutra (não "None não encontrada").
- Remoção de `from_event`: confirmar unused; remover função + teste.
- `learned`: `record` upsert por `match`; `list` imprime; `add` (keyword) acrescenta; corrompido→[].
  Chaves `{project_name, task_name, tag_names, billable}` consistentes com o item do `add`.
- `add` grava no `learned` no sucesso (não em dry-run); **isolar via `XDG_CONFIG_HOME`** todos os
  testes de `add` com POST real (mesma armadilha do C1 da v1.1).
- Migração: os testes de `overrides`/`suggest` removidos junto com os recursos.

## Migração

O autor resetou a config no dogfood; nenhum colega instalou ainda → **sem migração necessária**.
A seção `overrides` e o `history.json` de instalações v1.1 ficam órfãos (ignorados); documentar
que `learned.json` é o novo store. Não escrever código de migração (YAGNI).

## Plano de fases (visão)

1. **Default opcional** — `Defaults`/`load_defaults` tolerantes; `doctor` neutro; remover `from_event`. (TDD)
2. **Atividades aprendidas (motor)** — `learned.json` + módulo: `record`/`list`/`add` (keyword);
   `add` grava; remover `suggest`/`config add-override`/`overrides`. (TDD)
3. **Conversa** — `/horas`/`/lancar` leem `learned list`, reconhecimento pelo Claude, precedência,
   promoção com consentimento, linguagem leiga; tabela plain; esconder jargão.
4. **Onboarding** — reescrita do `SKILL.md` (3 perguntas, ICS/default opcionais, sem overrides).
5. **Verificação** — suíte verde, `claude plugin validate`, sem dado pessoal, versão segue 1.0.0.

## Fora de escopo (YAGNI)

- Algoritmo de matching fuzzy no código (o Claude faz).
- Inferir projeto pelo organizador/categoria do evento do Outlook.
- UI de gestão das atividades aprendidas (é um JSON; limpar = apagar/editar; o Claude ajuda).
- Migração de `overrides`/`history` v1.1.
- Bump de versão (segue 1.0.0 até o lançamento).
