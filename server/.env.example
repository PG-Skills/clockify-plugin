# Copie para .env na VPS e preencha. NÃO commitar o .env.
CLOCKIFY_MCP_DOMAIN=clockify.srv1625247.hstgr.cloud

# Chave AES-256 (base64 de 32 bytes) que cifra a chave do Clockify dentro do token:
#   python3 -c "import os,base64;print(base64.b64encode(os.urandom(32)).decode())"
CLOCKIFY_TOKEN_KEY=

# Segredo HS256 que assina os tokens OAuth:
#   openssl rand -hex 32
JWT_SECRET=
